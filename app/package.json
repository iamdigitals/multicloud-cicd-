{
  "name": "multicloud-cicd-demo",
  "version": "1.0.0",
  "description": "Multi-cloud CI/CD demo app — deployed via GitHub Actions to AWS and GCP",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
